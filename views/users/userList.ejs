<h2>User List</h2>
<ul>
    <% users.forEach(user => { %>
        <li>
            <a href="/users/<%= user._id %>/books"><%= user.name %> (<%= user.email %>)</a>
        </li>
    <% }) %>
</ul>

<a href="/">Back to Home</a>
