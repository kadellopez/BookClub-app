<div class="userl">
    <h2>User List</h2>
</div>
<ul>
    <% users.forEach(user => { %>
        <li>
            <a href="/users/<%= user._id %>/books"><%= user.name %> (<%= user.email %>)</a>
        </li>
    <% }) %>
</ul>

<div class="homeb">
<a href="/">Back to Home</a>
</div>